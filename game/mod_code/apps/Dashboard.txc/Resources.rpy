define -2 dashboard_background = "#e8edf9"

init python:
    register_feather_icon("clipboard", "")
    register_feather_icon("personnel", "")
    register_feather_icon("archive", "")

image dashboard_divider = Solid("#B44343", ysize=2)